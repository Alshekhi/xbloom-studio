# Pytest configuration for the xBloom Studio integration.
#
# The suite's configuration lives here: this repo is a Home Assistant
# integration rather than a Python package, so pytest.ini is its only home.
[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
