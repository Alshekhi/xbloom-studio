# Test-only dependencies.
#
# The suite does NOT install Home Assistant: tests/components/xbloom/conftest.py
# injects stub `homeassistant.*` modules, which keeps CI fast and avoids pinning
# the integration to one HA release. What is listed here is only what the
# component's own imports genuinely need at import time.
#
# This file exists because pyproject.toml is gitignored, so it cannot carry
# the dev extras.
pytest>=8
pytest-asyncio>=0.23

# The protocol library, now installed from PyPI rather than vendored. The
# [cloud] extra brings aiohttp and cryptography, which coordinator.py needs at
# import time because it imports xbloom.cloud unconditionally.
xbloom-py[cloud]==0.2.0

# xbloom.client + xbloom.cloud — HTTP transport.
aiohttp>=3.9

# xbloom.cloud imports this at module scope for the RSA request envelope, and
# coordinator.py imports cloud unconditionally. HA core ships it; without it
# here, tests/conftest.py stubs it.
cryptography>=42
