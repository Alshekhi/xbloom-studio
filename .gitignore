# Bridge runtime credentials — .env.example is committed; .env is not
bridge/.env

# Python generated files
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# macOS
.DS_Store

# Claude Code session state
.claude/

# Personal cleanup notes (host-specific)
CLEANUP.md
CLAUDE.md

# GSD planning artifacts
.planning/

# Generated graph output
graphify-out/

# Superseded — BLE replaced the cloud API bridge and standalone library
bridge/
/xbloom/

# Personal reference
docs/

# Standalone library build files (superseded — vendor copy lives in
# custom_components). tests/ is intentionally NOT ignored: it holds the
# spec/validation guards that keep the single source of truth honest.
pyproject.toml
uv.lock

# Virtual environment and dist
.venv/
dist/

# Local-only development artifacts (kept out of the repo)
discovery/*
!discovery/.gitkeep

# Local tooling
tools/
